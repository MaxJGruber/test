import { Typography } from "@mui/material";

{
  /* Pokemons inside this drawer */
}
const SampleDrawer = () => (
  <Typography variant="body1">I&apos;m a drawer</Typography>
);

export default SampleDrawer;
